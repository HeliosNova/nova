.env
*.db
__pycache__/
*.pyc
/data/
chromadb/
*.egg-info/
.pytest_cache/

# Security — never commit secrets or keys
*.key
*.pem
*.p12
*.pfx
credentials.json
service_account.json
*.secret

# IDE / editor
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# Claude Code
.claude/

# Local archive of internal/superseded docs — never public
.archive/

# DPO training data — generated locally from corrections, not repo content
training_data.jsonl
training_data.bak.*

# Node
node_modules/

# Build artifacts
dist/
build/
*.whl
finetune_progress.log
.tmp/

# Virtualenvs (never commit — huge, machine-specific)
.venv/
venv/
finetune_env/
finetune_env_py313_BROKEN/

# Model weights, caches, and training artifacts (huge / regenerable)
Qwen/
hf_cache/
unsloth_compiled_cache/
finetune_output/
finetune_output_27b/
local_train_data/
*.gguf
*.safetensors
*.bin
*.pt

# Logs and one-off diagnostic scripts
*.log
_phase0_*
_phase1_*

# Off-volume SQLite snapshots (bind-mounted into the container at /backups)
backups/

# SearXNG local settings (contains secret_key) — use settings.yml.example as template
searxng/settings.yml

# one-off audit/verification scratch scripts (not part of the app)
_audit/
