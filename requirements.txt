# Web framework
fastapi==0.135.1
uvicorn[standard]==0.34.0

# HTTP client
httpx==0.28.1

# Database
chromadb==0.6.3
# posthog >=6 broke capture()'s signature, spamming "Failed to send telemetry"
# from any fresh chroma client (chroma's own fix, PR #4970, is this pin; the
# 0.6.x line never got it). App telemetry is also off via ANONYMIZED_TELEMETRY.
# NOTE (audit 2026-08-24): do NOT bump chromadb to 1.x casually — 1.0.0-1.5.8
# carries CVE-2026-45829 (CVSS 10 pre-auth RCE in the server path); embedded
# 0.6.3 predates the vulnerable code entirely.
# NOTE (pip-audit 2026-08-29): 0.6.3 itself now reports three CVEs, and all
# three are SERVER-path only — they do not apply to this deployment:
#   CVE-2026-45830  cross-tenant read/write by an authenticated user
#   CVE-2026-45831  SimpleRBACAuthorizationProvider ignores tenant/db/collection
#   CVE-2026-45833  code injection via trust_remote_code on the collection
#                   UPDATE endpoint (/api/v2/tenants/.../collections/{id})
# Nova uses chromadb.PersistentClient EXCLUSIVELY (kg, learning, reflexion,
# retriever, skills) with no Chroma server container running, so there is no
# HTTP API, no tenants and no RBAC provider to attack. Upgrading to reach a
# "fixed" 1.x would trade three inapplicable findings for a real CVSS-10.
# Re-verify this reasoning if a Chroma SERVER is ever introduced.
posthog<6

# Math
sympy==1.13.3

# Utilities
# 0.0.31 (was 0.0.22, 2026-08-29): pip-audit found FIVE CVEs in 0.0.22 and the
# path is REACHABLE — Nova exposes UploadFile endpoints in api/documents.py,
# api/system.py (import_all) and api/voice.py, which FastAPI parses through this
# library. Not merely DoS:
#   PYSEC-2026-3037  parameter smuggling — ";" acted as a field separator, so
#                    "role=user&x=;role=admin" reads as role=user to an upstream
#                    validator but role=admin to the app (last value wins)
#   PYSEC-2026-3036  O(B^2) urlencoded parsing — a 1MiB "a;a;a;..." body costs
#                    ~1e11 byte comparisons and blocks a worker for seconds
#   PYSEC-2026-3039  unbounded part-header count/size -> CPU exhaustion
#   PYSEC-2026-3038  preamble/epilogue parsing DoS
#   PYSEC-2026-3040  negative Content-Length -> unbounded read
# 0.0.31 fixes all five; verified importable against the pinned fastapi 0.135.1.
python-multipart==0.0.31
pydantic>=2.11.0,<3.0.0
PyYAML>=6.0

# Channels
discord.py==2.4.0
python-telegram-bot==21.10

# Calendar
ics==0.7.2
tatsu==5.8.3

# Browser automation
playwright==1.49.1

# Desktop automation (optional — only used when ENABLE_DESKTOP_AUTOMATION=true)
pyautogui==0.9.54
# Floor raised 10.0.0 -> 12.3.0 (2026-08-31, pip-audit): 10.0.0 satisfies the
# old range but carries ~20 advisories (worst: CVE-2026-59197 heap OOB write,
# fixed 12.3.0). Live container already resolves 12.3.0 — zero-delta floor
# that stops a stale layer replay from landing inside the vulnerable window.
Pillow>=12.3.0

# Voice (optional — only used when ENABLE_VOICE=true)
openai-whisper==20250625
piper-tts==1.7.0

# MCP (Model Context Protocol) — pinned <2: mcp 2.0.0 (pulled by an unpinned
# rebuild 2026-08-12) removed the 1.x Server decorator API (`Server.list_tools`)
# that app/mcp_server.py targets, silently breaking the MCP feature + its tests.
# Lift the pin only together with a port of mcp_server.py to the 2.x API.
# Floor raised 1.23.0 -> 1.28.1 (2026-08-31, pip-audit): 1.23.0–1.28.0 carry
# CVE-2026-52869/-52870 (session hijack / cross-session task access, fixed
# 1.27.2) and CVE-2026-59950 (WS transport Origin bypass, fixed 1.28.1). All
# are in the SSE/HTTP/WS server transports — Nova's MCP server is stdio-only,
# so exposure was theoretical — but the safe floor costs nothing: the live
# container already resolves 1.29.1.
mcp>=1.28.1,<2

# Testing
pytest==8.3.5
pytest-asyncio==0.25.3
pytest-cov==6.1.1
